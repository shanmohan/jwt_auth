'use strict';

/**
 * @ngdoc function
 * @name jwtAuthApp.controller:LogoutCtrl
 * @description
 * # LogoutCtrl
 * Controller of the jwtAuthApp
 */
angular.module('jwtAuthApp')
  .controller('LogoutCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
